'''
모듈 : 미리 작성된 함수 코드를 모아둔 파이썬 파일
import lib
from lib import add
from lib as t
import math


print(math.pow(3,4)) # a의 b제곱을 하는 함수
print(math.sqrt(64)) # 제곱근을 구하는 함수
print(math.gcd(72,24)) # 최대 공약수를 구하는 함수

print(t.add(3,7))
'''
