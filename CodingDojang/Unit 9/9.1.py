a = '''
여러줄 출력은
" " 와 ' '
둘 다 사용이 가능하다
'''
aaa = """
이렇게도
사용가능
하다
"""

b = '"Hello world"' # ''로 문자열을 묶으면 내부에서 " "를 쓸 수 있다
c = "'Hello Python'" # 반대로 " "로 문자열을 묶으면 ' '를 쓸 수 있다
aa = 'Hello \'world\'' # 이스케이프 문자도 사용 가능
print(aa)
